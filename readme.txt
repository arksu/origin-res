Resource compiler for Origin-World game

для автоматической заливки ресурсов на сервер
требуется установленный WinSCP с явно прописанным путем до winscp.exe в системной переменной PATH
т.к. скрипт вызывает команду winscp.exe с параметрами.

xml utils взято отсюда : http://code.google.com/p/exgine/